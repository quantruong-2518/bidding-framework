# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc

# Environment
.env
.env.*
!.env.example

# Build
dist/
build/
.next/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Secrets
secrets/
*.pem
*.key

# Per-bid workspace artifacts (Phase 2.7) — written by workspace_snapshot_activity.
# The directory itself is kept (via .gitkeep) so the RW bind mount can attach on
# first `docker compose up`; individual bid subfolders are ephemeral.
src/kb-vault/bids/*/
